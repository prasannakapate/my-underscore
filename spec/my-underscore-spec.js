describe('My Underscore Library Function', function() {
    var a;

        it('it should return _.each() funcion', function() {
            expect(_.each([1,2,3],alert)).toBe(true);
        });

});
